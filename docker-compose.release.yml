services:
  api:
    image: ghcr.io/pin705/vidova:latest
    container_name: "vidova-api"
    ports:
      - "127.0.0.1:8000:8000"
    command: ["python3", "main.py"]
    volumes:
      - ./config.toml:/app/config.toml
      - ./storage:/app/storage
    restart: always
