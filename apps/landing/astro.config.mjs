import { defineConfig } from "astro/config";

// Static landing site. Deploy to Vercel / Cloudflare Pages / GitHub Pages.
export default defineConfig({
  site: "https://vidova-landing.vercel.app",
});
